extends layout

block content
  #articles
    - each article in articles
      .article
        .created_at= article.created_at
        .title
            a(href="/blog/"+article._id)!= article.title
        .body= article.body